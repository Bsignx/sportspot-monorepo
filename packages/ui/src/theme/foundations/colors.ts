import { colors as colorsTokens } from '@sportspot/tokens'

const brand = {
  100: colorsTokens.primary,
  200: colorsTokens.secondary,
  300: colorsTokens.tertiary,
}

const gray = {
  50: colorsTokens.grayscale50,
  100: colorsTokens.grayscale100,
  200: colorsTokens.grayscale200,
  300: colorsTokens.grayscale300,
  400: colorsTokens.grayscale400,
}

const black = colorsTokens.dark

const white = colorsTokens.light

const lineBorder = colorsTokens.lineBorder

const gradient = {
  100: colorsTokens.gradient1,
  200: colorsTokens.gradient2,
}

const colors = {
  brand,
  gray,
  black,
  white,
  gradient,
  lineBorder,
}

export default colors
